consul = "<%= @listener %>"
token = ""
retry = "<%= @retry %>"

# <%= @role %>
template {
  source = "<%= @template_source %>"
  destination = "<%= @template_dest %>"
  command = "<%= @template_cmd %>"
}

